name: ContainerChat
version: '1.0.0'
main: dev.containerchat.ContainerChat
api-version: '1.21'
description: Show container contents in chat by typing [container] or [c]
authors: [ ]

commands:
  containerchat:
    description: ContainerChat plugin command
    usage: /containerchat reload
    aliases: [ cc ]

permissions:
  containerchat.use:
    description: Allows players to use [container] in chat
    default: true
  containerchat.reload:
    description: Allows reloading the plugin config
    default: op
